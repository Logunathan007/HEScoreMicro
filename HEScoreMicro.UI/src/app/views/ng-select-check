<div class="col-md-6">
  <div class="select-box">
    <label> Manufactured Home Type </label>
    <ng-select
      [items]=""
      bindLabel="name"
      [clearable]="true"
      [closeOnSelect]="true"
      bindValue="value"
      placeholder="Manufactured Home Type "
      id="manufacturedHomeType"
      formControlName="manufacturedHomeType"
    >
    </ng-select>
    <div
      class="errormsg"
      *ngIf="
        (systemItem.get('manufacturedHomeType')?.touched ||
          systemItem.get('manufacturedHomeType')?.dirty) &&
        systemItem.get('manufacturedHomeType')?.invalid
      "
    >
      <label class="text-danger fw-semibold position-absolute">
        Manufactured Home Type is invalid
      </label>
    </div>
  </div>
</div>
